# Frontend Installation Instructions

1. Create a Next.js application:
```bash
npx create-next-app@latest frontend --typescript
cd frontend
```

2. Install Material UI and other dependencies:
```bash
npm install @mui/material @mui/icons-material @mui/lab @emotion/react @emotion/styled axios react-hook-form
```

3. Create a .env.local file:
```bash
touch .env.local
```

4. Add the following to your .env.local file:
